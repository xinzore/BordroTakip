# Copy this file to `keystore.properties` (do NOT commit it).
#
# storeFile can be an absolute path or a path relative to the repo root.
storeFile=bordrotakip-release.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=bordrotakip
keyPassword=YOUR_KEY_PASSWORD

